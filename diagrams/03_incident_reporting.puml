@startuml Incident Reporting Process
!theme plain
title Incident Reporting Process (FR18-FR24)

start

split
    :=== USER ACTIONS ===;
    :User accesses incident form;
    :Fill incident details;
    :Submit report;
    :System links to user profile;
    :Show success confirmation;
    :User receives confirmation;
split again
    :=== ADMIN ACTIONS ===;
    :Admin logs in;
    :Access admin dashboard;
    :View incident reports;
    :Select incident to review;
    :Update incident status;
    if (Status change?) then (Resolved)
        :Mark as resolved;
        :Generate resolution notification;
    else (In Progress)
        :Mark as in progress;
        :Generate update notification;
    else (Rejected)
        :Mark as rejected;
        :Generate rejection notification;
    endif
    :Trigger user notification;
    :User receives status update;
end split

:Process complete;

stop
@enduml
